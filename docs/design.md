# V4.2 intelligence and motion system

## Identity

The public identity is the full name **Hakan Duyar**. A circular computational nucleus acts as the abstract signature; no initial-based monogram is needed. Calibrated elliptical paths, phase markers, and a central signal event make it read as an active computational atom rather than a badge. This avoids ambiguity around `HD` and deliberately retires the rejected `HDU` and polygon/hexagon directions.

## Hybrid direction

- **Flight** provides aerospace calibration, motion, and technical confidence.
- **Signal** exposes relationships between interface, systems, delivery, operations, motion, and AI-assisted work.
- **Spatial** provides premium restraint through composed architecture layers and generous empty space.

The selected-systems scene combines a mission path with relationship links. Architecture uses spatial layers. Public telemetry combines disciplined measurement with a signal trace. Light mode is quieter and product-oriented; dark mode carries more of the aerospace/signal atmosphere.

## Master timeline

The animated hero is one SVG and one seamless 12-second loop. Idle motion uses periods that close on the same loop boundary, keeping the return frame calm and deterministic:

1. `00.0–02.3s` — Mode 01 / Flight calibration
2. `02.3–04.4s` — Flight → Signal: detect intersections, extract typed nodes, and infer relationships
3. `04.2–06.3s` — Mode 02 / Signal topology
4. `06.3–08.5s` — Signal → Spatial: cluster and classify relationships into architecture
5. `08.3–10.3s` — Mode 03 / Spatial architecture
6. `10.3–12.0s` — Spatial → Flight: converge layers, resolve coordinates, reacquire the nucleus, and calibrate orbit

Each transition owns the right-hand field while it is active, so modes do not visually pile up. Flight motion is reinterpreted as relationships, relationships are classified into architecture, and architecture is compressed back into calibrated motion. The identity nucleus remains stable across all modes. Reduced-motion users receive a separate static Flight asset rather than a paused animated document.

## Shared interpretation cycle

The lower scenes use the same original intelligence grammar without copying a film interface:

`ACQUIRE → TRACE → CLASSIFY → RESOLVE → QUIET`

- **Selected Systems** uses a 6.4-second cycle to acquire a source node, propagate through the mission trajectory, resolve four factual project nodes, briefly expose relations, and return to observation.
- **Architecture** uses a 6-second cycle to interpret Interface, State, Services, and Delivery as a spatially resolved software path.
- **Public Signal** uses a 6-second cycle to inspect a fixed telemetry trace, acquire a significant event, and resolve the factual language distribution without changing any real values.

Machine-like behavior is relational and temporary. Samaritan-like behavior is structural and decisive. Idle states remain legible and calm; transient diagnostics are explicitly non-essential.

## Responsive geometry

Systems, architecture, and public-signal scenes have dedicated 390 px vertical compositions. The responsive sources switch at 1080 px because GitHub's profile sidebar reduces the README column well before a conventional phone breakpoint. Desktop, intermediate, and mobile layouts preserve independent text zones, trajectory bands, nodes, and layer boundaries rather than shrinking one desktop drawing indiscriminately.

Every lower scene has animated desktop/mobile light/dark assets plus matching static fallbacks. Light mode carries more negative space and clinical structure; dark mode carries more signal depth. They share timing semantics, not identical visual density.
