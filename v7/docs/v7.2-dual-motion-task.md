# V7.2 dual-theme + corporate motion pass

Preserve the currently published V7.1 visual identity and composition. This is a refinement, not a redesign.

## Owner requirements

- Fix the remaining 390px mobile clipping/overflow without reducing any current font or logo size.
- Keep all content. Reflow and add height/space as needed.
- Restore both light and dark themes using `prefers-color-scheme` in the GitHub README.
- Light mode must be high-contrast, premium and as legible as dark mode; do not merely invert colours.
- Keep every visible word inside the generated SVG artwork. README must contain only a GitHub-safe `<picture>`/`<img>` composition and accessible alt text.
- Preserve the V7.1 exploded architecture, selected systems order and AI-assisted delivery path.
- Contact remains `iamhakanduyar@gmail.com` inside the artwork.

## Mobile correction

- No SVG text may extend outside `viewBox="0 0 390 …"`.
- No right-side architecture annotations may clip.
- Do not shrink fonts or logos. Move, wrap, stack, or increase vertical spacing/height.
- The phone must receive a true mobile SVG, not a scaled desktop poster.

## Motion and effects

Motion must be restrained, professional, corporate and technically meaningful:

- architecture connectors resolve in sequence;
- request/data path can travel downward through the five planes;
- project technology marks may reveal with a short stagger;
- AI delivery path progresses from specification to release;
- review can return to implementation through the repair loop;
- human gates receive a restrained emphasis only when reached.

No neon, particles, spinning logos, bouncing, playful easing, radar, glow everywhere or endless distraction. The fully resolved static state must be visible before/without animation. All animation must be CSS-only, GitHub-safe and scoped to `@media (prefers-reduced-motion: no-preference)`. Reduced-motion must show the final static design.

## Verification

- Build deterministic desktop/mobile × light/dark SVGs and PNG evidence.
- Run V7 QA, TypeScript and diff checks.
- Do not commit, push, merge or publish.
